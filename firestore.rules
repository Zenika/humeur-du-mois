service cloud.firestore {
  match /databases/{database}/documents {

    function authenticated() {
      return request.auth.uid != null;
    }

    match /employee-imports/{document=**} {
      allow read: if authenticated();
    }

  }
}
