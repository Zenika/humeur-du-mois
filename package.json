{
  "name": "humeur-du-mois-2018",
  "scripts": {
    "start": "npm --prefix functions run build && npm --prefix ui run build && npm run firebase serve",
    "deploy": "npm run firebase deploy",
    "firebase": "firebase"
  },
  "devDependencies": {
    "firebase-tools": "3.19.3"
  }
}
